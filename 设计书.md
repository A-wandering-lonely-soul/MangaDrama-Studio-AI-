AI 漫剧创作工作台 V1.0
产品与技术设计书

项目代号：MangaDrama Studio

项目定位：AI 漫剧可视化创作工作台

项目目标：
构建一个面向漫剧创作者的 Web / Desktop 创作工具，通过 Canvas/WebGL 渲染、Timeline 时间轴、Keyframe 动画、Camera 镜头系统以及 AI 内容生成能力，实现：

故事 → 剧本 → 分镜 → 素材 → 场景编排 → 动画 → 配音/字幕 → 漫剧预览

第一阶段不开发普通用户客户端、管理后台、社区系统，仅聚焦：

「创作者工作台」

1. 项目目标
1.1 核心目标

第一版实现一个类似：

Figma + 简化版剪映 + AI 漫剧生成器

的创作工具。

创作者可以：

创建漫剧项目
创建 Episode
创建 Scene
添加背景
添加角色
添加道具
添加文字
添加字幕
添加音频
调整图层
拖拽元素
缩放元素
旋转元素
调整 Camera
添加动画
添加 Keyframe
调整 Timeline
预览漫剧
保存项目
导出项目
AI 生成剧本
AI 生成分镜
AI 生成图片素材
2. V1 明确不做的功能

第一版禁止过度开发。

暂时不实现：

用户注册
用户登录
用户权限
会员
支付
推荐系统
社交
评论
点赞
弹幕
在线社区
创作者关注
完整管理后台
移动端 App
多人协作
实时协同编辑
完整视频剪辑能力
复杂音频编辑
AI Agent 自动完成全部制作流程

这些功能以后再做。

3. 产品形态

第一阶段：

                 MangaDrama Studio
                        │
             ┌──────────┴──────────┐
             │                     │
           Web                 Desktop
             │                     │
           React                  Tauri
             │                     │
             └──────────┬──────────┘
                        │
                   Shared Core
                        │
             ┌──────────┼──────────┐
             │          │          │
          Renderer    Timeline    Editor
             │          │          │
             └──────────┼──────────┘
                        │
                    Scene JSON
4. 技术栈
4.1 前端

必须使用：

React
TypeScript
Vite

状态管理：

Zustand

UI：

Tailwind CSS

路由：

React Router

数据请求：

TanStack Query
5. Canvas / Renderer 技术

核心渲染技术：

PixiJS

不要使用 React DOM 直接渲染漫剧画面。

架构：

React
   │
   ▼
Editor State
   │
   ▼
Scene JSON
   │
   ▼
Renderer
   │
   ▼
PixiJS
   │
   ▼
Canvas / WebGL

React 主要负责：

UI
Toolbar
Asset Panel
Inspector
Timeline
Project Panel

PixiJS 负责：

Scene
Sprite
Text
Animation
Camera
Effect
Particle
Transform
Hit Test
6. Renderer 设计原则

Renderer 必须与 React UI 解耦。

禁止：

React Component
      ↓
直接操作 Sprite

推荐：

React
 ↓
Scene State
 ↓
Renderer
 ↓
PixiJS

Renderer 只负责：

根据 Scene 数据渲染场景。

7. Scene Graph

整个漫剧由以下层级组成：

Project
 └── Episode
      └── Scene
           ├── Camera
           ├── Layers
           │    ├── Background
           │    ├── Character
           │    ├── Prop
           │    ├── Text
           │    └── Effect
           │
           ├── AudioTracks
           ├── SubtitleTracks
           └── AnimationTracks
8. Scene 数据模型

核心 TypeScript：

interface Project {
  id: string
  name: string
  version: string
  createdAt: number
  updatedAt: number

  episodes: Episode[]
}

interface Episode {
  id: string
  title: string
  scenes: Scene[]
}

interface Scene {
  id: string
  name: string

  width: number
  height: number

  duration: number

  camera: Camera

  objects: SceneObject[]

  audioTracks: AudioTrack[]

  subtitleTracks: SubtitleTrack[]

  animationTracks: AnimationTrack[]
}
9. SceneObject
interface SceneObject {
  id: string

  type:
    | 'background'
    | 'character'
    | 'prop'
    | 'text'
    | 'image'
    | 'video'
    | 'effect'

  name: string

  assetId?: string

  transform: Transform

  opacity: number

  visible: boolean

  locked: boolean

  zIndex: number
}
10. Transform
interface Transform {
  x: number
  y: number

  width: number
  height: number

  scaleX: number
  scaleY: number

  rotation: number

  anchorX: number
  anchorY: number
}

所有坐标使用：

World Coordinate

不要直接保存屏幕坐标。

11. Camera

Camera 是本项目的重要技术点。

interface Camera {
  x: number
  y: number

  zoom: number

  rotation: number
}

场景：

World
2000 × 2000

        ↓ Camera

Viewport
1080 × 1920

必须实现：

Pan
鼠标拖动
↓
Camera.x
Camera.y
Zoom
鼠标滚轮
↓
Camera.zoom
Camera Animation
0s
zoom = 1

        ↓

3s
zoom = 1.5
12. GIS 思维

本项目虽然不是 GIS 软件，但 Renderer 必须借鉴 GIS / 地图引擎的设计思想。

包括：

World Coordinate
Screen Coordinate
Viewport
Camera
Zoom
Pan
Layer
Spatial Transform
Hit Test
Bounding Box

未来可以扩展：

Scene Map

用于展示：

Scene 01
   ↓
Scene 02
   ↓
Scene 03
   ↓
Scene 04

第一版不接入 OpenLayers / Mapbox。

13. 坐标转换

必须实现：

worldToScreen()
screenToWorld()

例如：

function worldToScreen(
  point: Point,
  camera: Camera
): Point

以及：

function screenToWorld(
  point: Point,
  camera: Camera
): Point

这部分需要单独封装：

packages/renderer/src/coordinate/
14. 对象操作

Canvas 必须支持：

选择
Click
↓
Hit Test
↓
Select Object
移动
Drag
↓
Update Transform
缩放

支持：

8 个控制点
旋转

支持：

Rotation Handle
删除
Delete
Backspace
复制
Ctrl + C
Ctrl + V
多选
Shift + Click
框选
Mouse Drag
↓
Bounding Box
↓
Select Objects
15. 图层系统

必须支持：

Move Up
Move Down
Bring To Front
Send To Back

数据：

zIndex: number

示例：

100 Background

200 Building

300 Character

400 Effect

500 Subtitle
16. Asset 素材系统

素材类型：

Image
Character
Background
Prop
Audio
Video
Effect

目录：

assets/
├── characters/
├── backgrounds/
├── props/
├── audio/
├── video/
└── effects/

Asset：

interface Asset {
  id: string

  type:
    | 'image'
    | 'character'
    | 'background'
    | 'prop'
    | 'audio'
    | 'video'
    | 'effect'

  name: string

  url: string

  width?: number
  height?: number

  duration?: number

  metadata?: Record<string, unknown>
}
17. Timeline

Timeline 是核心模块之一。

结构：

Timeline
│
├── Camera Track
│
├── Character Track
│
├── Background Track
│
├── Subtitle Track
│
├── Audio Track
│
└── Effect Track

界面：

0s        5s        10s
│─────────│──────────│

Camera
●─────────●

Character
██████████████

Subtitle
     ███████

Audio
████████████████████
18. Timeline 数据模型
interface TimelineTrack {
  id: string

  type:
    | 'camera'
    | 'object'
    | 'subtitle'
    | 'audio'
    | 'effect'

  targetId?: string

  keyframes?: Keyframe[]
}
19. Keyframe
interface Keyframe {
  id: string

  time: number

  property:
    | 'x'
    | 'y'
    | 'scaleX'
    | 'scaleY'
    | 'rotation'
    | 'opacity'

  value: number
}

未来支持：

value: number | Point | Transform
20. 动画系统

必须实现基础：

Position
Scale
Rotation
Opacity
Camera

例如：

0s
x = -200

2s
x = 0

4s
x = 300

Renderer 根据：

currentTime

计算：

currentValue
21. 插值

第一版使用：

Linear Interpolation

公式：

value = start + (end - start) * progress

封装：

interpolate(
  start,
  end,
  progress
)

以后支持：

Ease In
Ease Out
Ease In Out
Bezier
22. 播放器

实现：

Play
Pause
Stop
Seek
Replay

核心：

currentTime: number
isPlaying: boolean

动画循环：

requestAnimationFrame
        ↓
currentTime
        ↓
Animation Engine
        ↓
Renderer
23. 播放器时间系统

不要依赖：

setInterval()

动画时间必须基于：

requestAnimationFrame
+
performance.now()

保证动画更加稳定。

24. 字幕系统
interface Subtitle {
  id: string

  startTime: number

  endTime: number

  text: string

  style: SubtitleStyle
}

支持：

字体
大小
颜色
描边
阴影
位置
透明度
25. Audio
interface AudioTrack {
  id: string

  assetId: string

  startTime: number

  duration: number

  volume: number

  loop: boolean
}

支持：

BGM
Voice
Sound Effect
26. AI 模块

AI 模块不要直接放在 React。

架构：

React
 ↓
NestJS
 ↓
AI Service
 ↓
Alibaba Cloud Model Studio

AI 能力：

Qwen
 ↓
剧本

Qwen
 ↓
分镜

Wan Image
 ↓
图片素材

Wan Video
 ↓
视频素材

阿里云百炼目前提供通义千问、万相等模型能力，并且万相图像/视频能力可以通过 API 接入。视频生成通常采用异步任务模式：提交任务后拿到 task_id，再查询任务状态和结果。具体模型、免费额度、价格和地域限制以阿里云百炼当前官方文档为准。

27. AI 剧本生成

用户：

一个少年在雨夜遇到了一个神秘女孩

AI：

Story
 ↓
Episode
 ↓
Scene

输出必须是：

结构化 JSON

不要让 AI 直接输出自然语言作为核心数据。

28. AI 分镜

输入：

Story

输出：

interface Storyboard {
  scenes: StoryboardScene[]
}

interface StoryboardScene {
  description: string

  duration: number

  camera: Camera

  characters: string[]

  background: string

  dialogue?: string

  mood?: string
}

然后转换：

Storyboard
 ↓
Scene
29. AI 素材生成

例如：

Storyboard
 ↓
Character Prompt
 ↓
Wan Image

得到：

character.png

背景：

Background Prompt
 ↓
Wan Image
 ↓
background.png

然后：

Asset Manager
 ↓
Scene
30. AI 生成任务必须异步

前端：

POST /api/ai/image

后端：

Alibaba Cloud
 ↓
taskId

前端：

GET /api/ai/tasks/:taskId

状态：

PENDING
RUNNING
SUCCEEDED
FAILED

不要让 HTTP 请求一直阻塞等待 AI 生成。

31. AI API 设计
POST /api/ai/story

POST /api/ai/storyboard

POST /api/ai/image

POST /api/ai/video

GET /api/ai/tasks/:taskId
32. 后端技术

第一版：

Node.js
NestJS
TypeScript

数据库：

SQLite

V1 不需要 PostgreSQL。

原因：

第一阶段重点是编辑器，不是后端业务。

以后正式平台再升级：

PostgreSQL
+
Redis
+
Object Storage
+
CDN
33. 本地项目存储

桌面端使用：

Tauri

项目结构：

MyDrama/
│
├── project.json
│
├── assets/
│   ├── characters/
│   ├── backgrounds/
│   ├── props/
│   ├── audio/
│   └── effects/
│
└── scenes/
    ├── scene-001.json
    ├── scene-002.json
    └── scene-003.json
34. Web 存储

Web 第一版：

IndexedDB

用于：

Project
Asset
Scene

不要第一版把大型图片直接塞进 LocalStorage。

35. 自动保存

编辑器必须支持：

修改
 ↓
Debounce
 ↓
Auto Save

例如：

用户停止操作 1 秒
 ↓
保存
36. Undo / Redo

编辑器必须实现：

Ctrl + Z

Ctrl + Shift + Z

状态：

History
 ├── Snapshot 1
 ├── Snapshot 2
 ├── Snapshot 3
 └── Current

第一版可以使用：

Command Pattern

或者：

Zustand + History

但必须保证：

Undo 不影响 Renderer 稳定性。

37. Zustand Store

建议：

stores/
├── projectStore
├── sceneStore
├── selectionStore
├── timelineStore
├── playbackStore
├── assetStore
└── editorStore

不要把所有状态塞进一个 store。

38. 编辑器布局

必须实现：

┌───────────────────────────────────────────────┐
│ Toolbar                                       │
├─────────────┬───────────────────┬─────────────┤
│             │                   │             │
│ Asset Panel │                   │ Inspector   │
│             │      Canvas       │             │
│             │                   │             │
│             │                   │             │
├─────────────┴───────────────────┴─────────────┤
│                                               │
│                  Timeline                     │
│                                               │
└───────────────────────────────────────────────┘
39. Toolbar

包含：

New
Open
Save
Undo
Redo

Select
Hand
Text

Play
Pause

Zoom In
Zoom Out
Fit

AI
Export
40. Asset Panel

左侧：

素材
│
├── 我的素材
├── 角色
├── 背景
├── 道具
├── 音乐
├── 音效
└── 特效

支持：

Upload
Drag & Drop
Delete
Rename
Preview
41. Inspector

右侧：

Transform

X
Y

Width
Height

Scale

Rotation

Opacity

Layer

Animation

选中不同类型显示不同属性。

42. Canvas 交互

Canvas 必须支持：

Select
Move
Resize
Rotate
Pan
Zoom

快捷键：

V 选择

H 平移

Delete 删除

Ctrl + C
Ctrl + V

Ctrl + Z
Ctrl + Shift + Z

Space + Drag
Pan
43. 响应式

创作端优先：

Desktop Layout

Web 主要针对：

1280 × 720
1440 × 900
1920 × 1080

第一版不强求手机浏览器适配。

44. Tauri

第二阶段接入：

React
 ↓
Vite
 ↓
Tauri 2

桌面端支持：

Windows
macOS
Linux

桌面端额外能力：

本地文件
文件夹
项目文件
拖拽
离线编辑
自动保存
45. Monorepo

建议使用：

pnpm
+
Turborepo

目录：

manga-drama/
│
├── apps/
│   ├── web/
│   └── desktop/
│
├── packages/
│   ├── renderer/
│   ├── timeline/
│   ├── editor/
│   ├── types/
│   ├── utils/
│   └── ui/
│
├── server/
│
├── docs/
│
├── package.json
└── pnpm-workspace.yaml
46. Renderer Package
packages/renderer/

src/
├── core/
│   ├── Renderer.ts
│   ├── Scene.ts
│   └── Layer.ts
│
├── camera/
│   ├── Camera.ts
│   └── viewport.ts
│
├── coordinate/
│   ├── worldToScreen.ts
│   └── screenToWorld.ts
│
├── objects/
│   ├── SpriteObject.ts
│   ├── TextObject.ts
│   └── VideoObject.ts
│
├── animation/
│   ├── AnimationEngine.ts
│   ├── interpolation.ts
│   └── keyframe.ts
│
├── effects/
│   └── particle/
│
└── index.ts
47. Timeline Package
packages/timeline/

src/
├── Timeline.tsx
├── Track.tsx
├── Keyframe.tsx
├── Ruler.tsx
├── Playhead.tsx
├── TimelineEngine.ts
└── types.ts
48. Editor Package
packages/editor/

src/
├── CanvasEditor.tsx
├── SelectionBox.tsx
├── TransformControls.tsx
├── AssetPanel.tsx
├── Inspector.tsx
├── Toolbar.tsx
└── shortcuts.ts
49. 开发阶段

严格按照下面顺序开发。

Phase 0：工程初始化

目标：

React
TypeScript
Vite
pnpm
Turborepo
Tailwind
Zustand
PixiJS

完成：

Monorepo
Web
Shared Packages
Phase 1：Pixi Renderer

目标：

让一个人物图片能够出现在 Canvas 中。

实现：

Pixi Application

Scene

Sprite

Layer

Camera

Zoom

Pan

验收：

打开页面
 ↓
Canvas
 ↓
添加图片
 ↓
图片出现
 ↓
鼠标拖动
 ↓
图片移动
Phase 2：Canvas Editor

实现：

Select
Move
Resize
Rotate
Delete
Duplicate
Multi Select
Layer

验收：

能够像简化版 Figma 一样操作场景中的人物。

Phase 3：Timeline

实现：

Timeline
Track
Playhead
Keyframe
Playback

验收：

人物：

0s → x = 0

2s → x = 500

播放

人物从左移动到右。
Phase 4：Camera

实现：

Camera
Zoom
Pan
Camera Keyframe

验收：

镜头：

0s
zoom = 1

3s
zoom = 1.5

播放时镜头自动推进。

Phase 5：字幕 / 音频

实现：

Subtitle
Audio
BGM
Sound Effect

验收：

播放

人物
+
字幕
+
音乐

同步。

Phase 6：项目保存

实现：

project.json
scene.json
assets

Web：

IndexedDB

Desktop：

Tauri File System
Phase 7：AI

接入：

Qwen
Wan Image

实现：

输入故事
 ↓
AI 剧本
 ↓
AI 分镜
 ↓
AI 图片
 ↓
自动加入 Asset
Phase 8：AI → Scene

最终实现：

用户：

“一个少年在雨夜遇到神秘女孩”

       ↓

      AI

       ↓

Story
       ↓
Storyboard
       ↓
Scene JSON
       ↓
Assets
       ↓
Renderer
       ↓
漫剧
Phase 9：Tauri

将：

Web

打包：

Windows
macOS
Linux

实现：

本地项目
本地素材
文件拖拽
自动保存
离线编辑
50. V1 最终效果

用户打开：

MangaDrama Studio

创建：

《雨夜》

输入：

一个少年在雨夜遇见神秘女孩。

点击：

AI 生成

得到：

Scene 01

背景：雨夜城市

人物：
少年
女孩

字幕：
“你终于来了……”

Camera：
Slow Zoom

BGM：
Rain

Canvas：

┌──────────────────┐
│ 🌧️               │
│                  │
│      🧍          │
│                  │
│           👩     │
│                  │
│ “你终于来了……”  │
└──────────────────┘

点击播放：

人物移动
+
Camera Zoom
+
雨滴粒子
+
字幕
+
BGM

最终形成：

一个真正可以“动起来”的漫剧场景。

51. 性能要求

Renderer 必须考虑性能。

原则：

React
不参与每帧动画

禁止：

60fps animation
 ↓
setState()
 ↓
React Render

应该：

requestAnimationFrame
 ↓
Animation Engine
 ↓
PixiJS

React 只负责编辑状态。

52. 大量对象

如果场景存在：

100+
Sprite

需要考虑：

Object Pool
Texture Cache
Sprite Reuse
ParticleContainer

图片：

Texture Cache

避免重复加载。

53. Worker

后期可以把：

AI JSON parsing
大量数据处理
视频处理
缩略图生成

放入：

Web Worker

但 V1 不要为了使用 Worker 而使用 Worker。

54. 错误处理

AI：

PENDING
RUNNING
SUCCESS
FAILED

必须给用户：

Loading
Retry
Error

Renderer：

Asset Load Failed

不能让整个编辑器崩溃。

55. TypeScript 要求

禁止：

any

除非：

// eslint-disable-next-line

并且必须解释原因。

所有核心数据：

Project
Episode
Scene
SceneObject
Camera
Animation
Timeline
Asset

必须拥有明确 TypeScript 类型。

56. 代码规范

使用：

ESLint
Prettier
TypeScript Strict

组件：

PascalCase

变量：

camelCase

类型：

PascalCase
57. Git

Commit：

feat:
fix:
refactor:
docs:
perf:
chore:

例如：

feat: implement pixi scene renderer

feat: add timeline keyframe system

feat: add camera animation

fix: resolve canvas coordinate transform issue
58. AI Agent 开发规则

这是给 Coding Agent 的重要规则。

Rule 1

不要一次实现整个项目。

必须按照：

Phase 0
 ↓
Phase 1
 ↓
Phase 2
 ↓
...

逐阶段完成。

Rule 2

每个阶段完成以后：

运行
 ↓
测试
 ↓
修复
 ↓
确认

再进入下一阶段。

Rule 3

不要擅自修改核心架构。

尤其不能随意改变：

Scene JSON
Renderer
Timeline
Camera
Rule 4

React 不负责每帧动画。

Rule 5

Renderer 与 UI 解耦。

Rule 6

所有核心功能必须有 TypeScript 类型。

Rule 7

优先实现核心能力，不要过度美化 UI。

优先级：

功能
>
架构
>
性能
>
交互
>
视觉
59. V1 验收标准

最终至少完成：

编辑器
 Canvas
 PixiJS
 图片添加
 图片删除
 图片拖拽
 图片缩放
 图片旋转
 多选
 图层
Camera
 Zoom
 Pan
 Camera Animation
Timeline
 Track
 Playhead
 Keyframe
 Playback
 Seek
Animation
 Position
 Scale
 Rotation
 Opacity
内容
 Background
 Character
 Prop
 Text
 Subtitle
 Audio
数据
 Project
 Episode
 Scene
 Asset
 Save
 Load
 Undo
 Redo
 Auto Save
AI
 Story
 Storyboard
 Image Generation
 Async Task
 Asset Import
Desktop
 Tauri
 Open Project
 Save Project
 Local Assets
60. 最终简历项目描述

项目名称：

MangaDrama Studio — AI 漫剧可视化创作平台

简历可以写：

基于 React + TypeScript + PixiJS + Tauri 构建跨端 AI 漫剧创作工作台，实现二维 Scene Graph、Camera/Viewport、Timeline/Keyframe 动画及素材管理能力。

设计 Renderer 与 React 编辑状态解耦架构，通过统一 Scene JSON 描述场景、图层、角色、字幕、音频及动画，实现 Web 与 Desktop 渲染逻辑复用。

基于 PixiJS 实现 Canvas/WebGL 场景编辑器，支持对象拖拽、缩放、旋转、多选、图层管理、坐标转换、Camera 漫游及粒子特效。

实现基于 requestAnimationFrame 的 Timeline 播放引擎和 Keyframe 插值系统，支持人物移动、镜头缩放、透明度、旋转等动画效果。

接入阿里云百炼 Qwen/万相模型，实现“故事 → 剧本 → 分镜 → AI 素材 → Scene”的 AI 漫剧生成流程，并通过异步任务机制处理 AI 图片/视频生成。

61. 最终技术架构
                         ┌──────────────────┐
                         │   MangaDrama     │
                         │      Studio      │
                         └────────┬─────────┘
                                  │
                     ┌────────────┴────────────┐
                     │                         │
                    Web                     Desktop
                     │                         │
                   React                     Tauri
                     │                         │
                     └────────────┬────────────┘
                                  │
                           Shared Packages
                                  │
            ┌─────────────────────┼─────────────────────┐
            │                     │                     │
         Renderer              Timeline              Editor
            │                     │                     │
         PixiJS              Keyframe             Zustand
            │                     │                     │
            └─────────────────────┼─────────────────────┘
                                  │
                              Scene JSON
                                  │
                         ┌────────┴────────┐
                         │                 │
                      Local             Backend
                         │                 │
                     IndexedDB          NestJS
                         │                 │
                         │           ┌─────┴─────┐
                         │           │           │
                         │          Qwen        Wan
                         │           │           │
                         │        Script      Image
                         │        Storyboard  Video
                         │
                         └──────────────┬──────────────
                                        │
                                    Assets
                                        │
                                  Object Storage
62. 最终产品演进路线
V1
│
├── Canvas Editor
├── Timeline
├── Animation
├── Camera
└── Asset
     │
     ▼
V2
│
├── AI Script
├── AI Storyboard
├── AI Image
└── AI Scene
     │
     ▼
V3
│
├── AI Video
├── Voice
├── BGM
└── Export
     │
     ▼
V4
│
├── Scene Map
├── Multi Scene
├── Project Management
└── Tauri Desktop
     │
     ▼
V5
│
├── Collaboration
├── Cloud Project
├── Creator Community
└── User Client
给 AI Agent 的最终任务

你现在不是在开发一个普通的 React CRUD 项目，而是在开发一个轻量级的 AI 漫剧创作软件。

第一目标不是页面数量，而是建立稳定的 Scene Graph + Renderer + Camera + Timeline + Keyframe 核心架构。

请严格按照本设计书的 Phase 0 → Phase 9 顺序执行。

每完成一个 Phase，先运行项目、进行基础测试、解决 TypeScript / ESLint / Runtime 错误，再继续下一阶段。

不允许跳过核心架构直接堆 UI。

不允许让 React state 驱动 60fps Canvas 动画。

不允许把 AI API Key 放到前端。

不允许使用 any 作为逃避类型设计的手段。

不允许为了“看起来完成”而虚构尚未实现的 AI API。

所有无法确认的阿里云百炼模型名称、API 参数、免费额度、地域限制，必须以当前官方文档为准，并在接入前进行验证。

优先保证核心编辑器可以独立运行，即使暂时没有 AI，也必须能够通过本地素材完成漫剧制作。